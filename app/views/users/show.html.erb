<% provide(:title, @user.name) %>
<h1>
	<%= gravatar_for @user %>
	<%= @user.name %>
</h1>
<% if @user.documents.any? %>
    <h3>Documents (<%= @user.documents.count %>)</h3>
    <ol class="documents">
    	<%= render @documents %>
    </ol>
    <%= will_paginate @documents %>
<% end %>